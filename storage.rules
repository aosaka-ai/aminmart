rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read access to all files
      allow read: if true;
      
      // Allow write access to authenticated users
      // You can further restrict this to admins only if needed
      allow write: if request.auth != null;
    }
  }
}
